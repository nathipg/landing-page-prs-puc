export interface ThemeType {
  color: {
    primary: string;
    bgPrimary: string;
    bgSecondary: string;
  };
  breakPoint: {
    sm: string;
    md: string;
    lg: string;
    xl: string;
    xxl: string;
  };
}
